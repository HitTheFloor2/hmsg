1.发送消息-等待回复的实现：
    有的场景需要发送一个消息并且等待一个或者数个消息的返回（比如发起一次投票）。
    此时对于回复者不需要考虑同步问题，但是对于发送者需要同步。比如发送给n和节点并且等待所有的节点进行同步。
    当前的实现方法为：发送方在开始等待之前，注册一个同步器（如Countdownlatch），同步完成之后销毁
2.多线程调用static方法：
    当不存在全局变量时是安全的
3.实现全局可访问的对象
    static